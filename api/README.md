# API Service

Deno (≥2.5) + Hono + TypeScript RAG orchestration service

## Development

```bash
deno run --allow-net --allow-env --allow-read --watch main.ts
```

## Build

```bash
deno compile --allow-net --allow-env --allow-read -o api main.ts
``` 