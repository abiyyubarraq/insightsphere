# Shared Utilities

Common types and utilities used across the InsightSphere monorepo.

## Structure
 
- `types/` - TypeScript type definitions
- `utils/` - Common utility functions
- `constants/` - Application constants 